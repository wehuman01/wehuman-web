---
interface Props {
  t: any;
  lang: string;
  current?: 'home' | 'about';
}

const { t, lang, current } = Astro.props;
const basePath = import.meta.env.BASE_URL.replace(/\/$/, '');
const withBase = (path: string) => `${basePath}${path}`;
const homeHref = withBase(lang === 'zh' ? '/zh/' : '/');
const aboutHref = withBase(lang === 'zh' ? '/zh/about' : '/about');
const installHref = `${homeHref}#install`;

// Build lang-switch href from current path
const currentPath = Astro.url.pathname.replace(/\/$/, '') || '/';
let langHref: string;
if (lang === 'zh') {
  langHref = currentPath.replace(/^\/zh(\/|$)/, '/') || '/';
} else {
  langHref = currentPath === '/' ? '/zh/' : `/zh${currentPath}`;
}
langHref = withBase(langHref);
---

<!-- 三栏顶栏：左链接 · 中字标 + 图标 · 右语言 + 安装 -->
<nav class="hw-nav">
  <div class="hw-nav__side">
    <a href={homeHref} class="hw-nav__link" aria-current={current === 'home' ? 'page' : undefined}>
      {t.nav.home}
    </a>
    <a href={aboutHref} class="hw-nav__link" aria-current={current === 'about' ? 'page' : undefined}>
      {t.nav.about}
    </a>
  </div>

  <div class="hw-nav__center">
    <a href={homeHref} class="hw-nav__wordmark" aria-label="wehuman home">WEHUMAN</a>
    <span class="hw-nav__icons">
      <a href={t.links.github_org} target="_blank" rel="noopener" aria-label="GitHub" title="GitHub">
        <svg width="22" height="22" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
          <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27s1.36.09 2 .27c1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.01 8.01 0 0 0 16 8c0-4.42-3.58-8-8-8Z"></path>
        </svg>
      </a>
    </span>
  </div>

  <div class="hw-nav__side hw-nav__side--right">
    <a href={langHref} class="hw-nav__lang">{lang === 'zh' ? 'EN' : '中文'}</a>
    <a href={installHref} class="hw-nav__link">
      {t.nav.install}
      <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" style="vertical-align:-1px">
        <path d="M7 17 17 7"></path>
        <path d="M8 7h9v9"></path>
      </svg>
    </a>
  </div>
</nav>
